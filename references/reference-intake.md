# 参考图校准方法

当用户提供参考图、失败结果或新的偏好反馈时使用本文件。

## 素材分类

允许一张图同时属于多类。记录时使用以下标签：

- `C` 构图：只学习面积关系、主体位置、视觉路径、留白、层次和裁切方式。
- `P` 配色：只学习橙色占比、冷色角色、明度、饱和度、材质与颜色的对应关系。
- `F` 摄影/胶片：只学习光线、镜头、颗粒、高光、阴影和色彩响应。
- `N` 反例：记录不喜欢的具体原因，作为负向边界，不从单张失败图推导普遍规则。

## 建议数量

参考图可以跨组重复使用，不需要为了凑数量寻找完全不同的图片。

| 分组 | 最低可用 | 推荐数量 | 主要用途 |
| --- | ---: | ---: | --- |
| 构图 `C` | 6 张 | 10-15 张 | 学习主体位置、面积关系、视觉路径、留白和层次 |
| 配色 `P` | 6 张 | 10-15 张 | 学习橙色占比、冷色层级、明度和饱和度关系 |
| 摄影/胶片 `F` | 4 张 | 6-10 张 | 学习光线、镜头、颗粒、高光与阴影质感 |
| 反例 `N` | 3 张 | 5-8 张 | 划定俗艳、突兀、塑料感、CG 感等负向边界 |

首轮不必一次备齐推荐数量。先提供最低数量完成一次校准，再用生成结果暴露的问题补充参考，通常比无差别收集大量图片更有效。

## 每张图的标注

每张图附一行即可：

`编号｜标签 C/P/F/N｜最想保留的点｜最不想复制的点｜重要程度 1-3`

示例：`07｜C+P｜大面积浅蓝留白与右下角橙色小主体｜不要建筑造型本身｜3`

若没有时间逐张标注，至少按四组分开上传，并为每组写一句总体偏好。没有标注时只能把规律记为待验证倾向。

## 每张图的提取字段

1. 用户最喜欢或最不喜欢的一个具体特征。
2. 主体和橙色锚点的位置、面积、载体及数量。
3. 主构图系统、视觉路径、最大负空间和前中后景关系。
4. 冷色的角色分工，以及中性色是否参与缓冲。
5. 主光方向、软硬、反差、材质反射和时间感。
6. 焦段倾向、机位高度、景深和透视特征。
7. 颗粒、高光晕染、阴影层次及不应复制的瑕疵。
8. 对 9:16 裁切和手机安全区是否友好。

若用户指定“只分析构图和摄影质感”，本轮只填写 `C` 与 `F` 字段：

- `C`：主场域面积、主体位置、重复结构、唯一偏离点、主视觉路径、视角压缩、负空间和裁切余量。
- `F`：光线模式、方向性、反差、镜头/机位倾向、景深、材质微纹理、清晰度、颗粒和高光滚降。
- 明确标记不应泛化的特殊后期（如拖影、扫描线、glitch），不要把它们并入默认胶片质感。

若用户指定“只分析配色”，本轮只填写 `P` 字段：

- `P`：主冷色与辅冷色、冷色主场比例、橙色载体与面积区间、色相/饱和度/明度层级、中性色缓冲、颜色漂移和负向边界。
- 用确定性调色板或等价的颜色提取方法辅助判断，但把聚类比例视为参考指标，不当作精确像素面积。
- 至少区分“默认锚点”“扩大结构面”和“颜色漂移反例”；不因单张全橙图提高默认橙色比例。

## 归纳规则

- 优先采纳用户的文字说明，不把“用户发来”自动等同于“用户喜欢全部元素”。
- 同一规律在至少 3 张正向参考中重复出现，才记录为高置信偏好。
- 完全相同或仅重复上传的图片只计权一次；同一构图的真实变体可分别记录，但不得用近重复图虚增置信度。
- 只出现 1-2 次的规律记录为待验证倾向，不写成强制规则。
- 参考之间冲突时，保留两个可命名的视觉方向，不做平均化混合。
- 学习视觉语法而非复制具体物体、人物身份、作品标识或独特场景。
- 每批分析后更新 [偏好档案](calibration-profile.md)，注明依据数量与置信度。
- 公开作品集只能作为构图/摄影质感候选来源；若无法核验原始平台的广告或付费合作状态，记录为 `unknown`，不得声称已完成广告排除。

## 向用户索取素材时

优先请用户按“构图、配色、胶片质感、反例”四组上传，并为每张图附一句喜欢或不喜欢的原因。若素材不足，先按默认视觉系统生成，不阻塞普通任务；只有在多个方向会显著改变结果时才追问。

除图片外，首轮还应确认：

- 默认平台固定为 Midjourney v8.2，默认语言固定为英文；只有用户明确覆盖时才记录其他平台或语言要求。
- 主要题材范围，以及人物、文字、Logo、建筑、产品等内容禁区。
- 用于锁屏、主屏还是两者兼顾；若有固定手机型号，可提供截图模板。
- 每次希望生成的方向数，以及是否需要简短 `--no` 词和平台参数；每条提示词默认不超过 900 个字符。
