# Midjourney v8.2 适配

本文件定义统一视觉方案如何编译为一条可直接粘贴的 Midjourney v8.2 英文提示词。视觉内容以 [偏好档案](calibration-profile.md) 和 [视觉系统](visual-system.md) 为准；平台适配不得改变主体位置、橙色面积、冷色静区、光线模式或锁屏安全区。

## 输出目标

- 每个方向只输出一条英文、连续描述的 Midjourney prompt。
- 每条提示词（含空格、标点和平台参数）不超过 900 个字符。
- 参数固定放在末尾：`--ar 9:16 --raw --v 8.2`。
- 不默认输出中文翻译、GPT Image 2 版本、JSON、解释段落或独立 negative prompt。

## 提示词顺序

按以下顺序组织一段紧凑的摄影描述：

1. 成年人物/花卉主体、身份、服装轮廓和真实摄影类型。
2. 画面中的位置、景别、非对称姿态证据、杂志级版式/裁切、构图系统、视觉路径和负空间。
3. 橙色载体、`bright traffic orange (#ff4900)`、位置和面积模式：默认 `15%-20%`，自然载体可用 `10%-25%` 容差；`30%-45%` 或超过 `50%` 只有用户明确要求时才覆盖默认值。
4. 浅蓝、深蓝、青色和中性色的角色分工，冷色静区保持主场。
5. 光源方向、软硬度、材质响应、机位、透视、景深和电影胶片质感。
6. 完整画面的边缘裁切余量，以及用户明确指定锁屏/设备模板时的界面安全区和必要的无文字约束；未指定时不强制顶部留白。
7. 平台参数置于最后。

不要把 `High Fashion Editorial` 或 `Scale Shift` 作为孤立标签；把它们展开为服装轮廓、动作、裁切、物体关系和摄影条件。避免罗列相机品牌、`8K`、`masterpiece`、`ultra detailed` 等不可观察的空泛词。

## 非对称姿态与杂志级构图

- 含人物时，至少写出一个有支撑的非对称证据（自然重心偏移、肩胯反向、躯干轻扭、左右手不等量、偏轴视线或单侧遮挡）和一个具体的视线/手部动作；不要只写 `asymmetrical pose`。
- 每条人物提示词选择一种杂志级版式：偏置主体、边缘裁切、对角视觉路径、前景遮挡或大面积负空间。保持一个主体、一个橙色锚点和一块安静背景的主次，避免居中填满和电商目录感。
- 姿态、版式与 9:16 边缘裁切协同，不能遮住橙色锚点或关键接触关系；只有用户指定锁屏时才额外避开手机时钟区。

## Scale Shift 的可选用法

在多个方向中，默认最多选择一个方向使用 Scale Shift；其他方向可使用悬浮结构、双层空间、不连续入口、线条投影或写实关系重构。未选择 Scale Shift 的提示词不要加入 `giant`、`miniature` 或强制比例说明。

启用 Scale Shift 时，必须用简短语句交代相对比例、接触/承重、重心、透视、投影和材质反应，并且只保留这一种主要异常机制。人体解剖、姿态、面部身份、花瓣结构和物理光线仍须可信。

## 字符压缩策略

先完成视觉信息，再按以下优先级压缩：

1. 删除 `beautiful`、`luxury`、`stunning`、`cinematic` 等不增加画面信息的词。
2. 合并重复的写实、胶片、冷色和安全区约束。
3. 用一个具体材质或动作替代多个同义形容词。
4. 删除不必要的相机品牌、精确器材型号和重复负面词。
5. 保留主体、空间关系、非对称姿态证据、杂志级版式层级、橙色载体、面积、异常机制（若有）、光线、摄影质感和平台参数。

最终以实际字符计数为准，不能用“约 900 字符”代替检查。

## `--no` 规则

只把适合独立读取且不会误伤主体的名词放入简短参数，例如：

`--no text, logo, watermark, CGI, plastic skin, duplicate face`

颜色漂移、橙色面积、构图限制和是否启用 Scale Shift 写在正向描述中。不要在 `--no` 中使用 `red`、`flower`、`wall`、`clothing` 等宽泛词。

## 交付检查

- 每个方向只有一条英文 prompt，且总字符数不超过 900。
- `--ar 9:16 --raw --v 8.2` 位于末尾，没有后置文本。
- 方案之间至少改变两个结构变量；Scale Shift 只在被选中的方向出现。
- 手机缩略图能读出人物、橙色锚点和主要空间关系，且无文字、Logo 或水印。
- 人物是否以非对称但有支撑的姿态出现，且杂志级版式可通过偏置、裁切、对角线、遮挡或负空间读出。
