# 视觉系统

生成提示词前用本文件确定画面结构。不要把所有选项同时写入同一条提示词。当前系统已根据首批 15 张唯一参考图完成校准；详细依据见 [2026-08-24 校准记录](calibration-2026-08-24.md)。

若主题包含人物与植物、花卉、叶片、枝条、树冠、产品或雕塑，或用户提出 `High Fashion Editorial` / `Scale Shift`，先阅读 [人物—植物/物体编辑美学](editorial-scale-shift.md)，再从本文件选择橙色载体、冷色层级、构图系统和光线模式。

含人物时，人物层默认采用非对称但有支撑的姿态，并配合一种可读的杂志级版式；不要与对称填充或中心目录构图并用。

## 1. 橙色视觉锚点

将亮橙色写成具体、可信、可定位的视觉载体，例如：

- 建筑或工业场景：门、栏杆、金属板、交通标记、局部灯箱、涂层构件。
- 自然或户外场景：帐篷、救生设备、布料、花朵、阳光照亮的局部物体。
- 产品或静物场景：透明树脂、漆面金属、陶瓷、织物、液体或包装结构。
- 抽象摄影场景：有真实材质依据的亚克力、玻璃、薄膜、折纸或建筑表皮。

默认使用结构面模式，同时保留两种面积变体：

- `默认结构面`：橙色约占 `15%-20%`，作为下方横向色带、画面边缘的竖向色带、单一几何平面、较大服装轮廓或具有真实材质的超现实物体；自然植物载体可在 `10%-25%` 内调整，冷色静区仍应占至少约 `55%`。
- `小锚点变体`：仅在用户明确要求“小面积点缀”时使用，橙色约占 `3%-10%`，以单一物体、人物衣物、细线、栏杆或远处构件出现。
- `扩大结构面变体`：仅在用户明确要求更多橙色时使用，占比可从 `20%` 扩展至 `30%`，但不得破坏整体构图平衡或压缩冷色静区；锁屏顶部界面只在用户明确指定时保护。

优先使用“一个主锚点 + 最多一个低强度回声”。橙色回声可以是反射、边缘光或远处小物体，不应形成多个同等抢眼的亮点。只有用户明确选择“大面积橙色”方向时，才允许橙色超过约 `30%`，且仍需用浅蓝或青色保留呼吸空间。

描述颜色时同时给出名称与色值，例如“明亮交通橙 `#ff4900`”。色值是约束，不意味着画面中每个橙色像素完全一致；真实光照下应允许高光和阴影变化，但基准色相不得漂成正红、猩红、酒红或洋红。

### 1.1 配色批次校准

第一批 19 张配色参考支持以下色彩决策：

- 冷色主场优先占约 `55%-80%`；常见基底为浅青/浅蓝、深蓝或蓝绿色。选择一个主冷色和一个辅冷色，不把所有冷色做成等饱和度拼盘。
- `#ff4900` 默认占约 `15%-20%`，是单一高饱和暖色锚点；自然载体可在 `10%-25%` 内调整。`30%-45%` 仅用于用户明确要求的扩大结构面，超过 `50%` 仅用于实验。
- 亮度关系优先于颜色数量：让冷色基底降低饱和度或承担较暗/较亮的明度层，橙色承担最高的暖色识别度，中性色只做少量缓冲。
- 明确排除红、砖、锈、棕和酒红漂移；生成提示词中写出 `bright traffic orange (#ff4900), saturated warm orange, not red, brick, rust or brown`。

可从以下模板中选择一个，不要同时混用：

- `清透青蓝 + 橙色锚点`：浅青/浅蓝约 `60%-80%`，橙色约 `12%-20%`，少量白/灰缓冲。
- `深蓝静区 + 橙色结构面`：深蓝约 `55%-75%`，橙色约 `15%-25%`，用明度差建立强对比。
- `蓝绿色场 + 橙色自然主体`：青绿/湖蓝约 `60%-80%`，橙色花、果实或叶片约 `15%-25%`，不加入第二种高饱和暖色。
- `扩大橙色结构面`：橙色约 `30%-45%`，只有用户明确要求时启用，并保留深蓝或浅蓝反衬。

## 2. 冷色角色分工

- 浅蓝：空气感、天空、雾、玻璃、柔和负空间，通常低饱和、高明度。
- 深蓝：天空、背景、结构面、深水或阴影，优先使用有色彩层次的钴蓝、海军蓝或普鲁士蓝，不用无层次纯黑替代。
- 青色：天空、墙面、水面、玻璃反射或过渡层，可以成为主静区；保持清透或略带灰度，避免霓虹赛博朋克感。
- 低饱和鼠尾草绿：用于自然场景的草地、坡地和植被，可与雾蓝或深青天空组成第三套冷色基底。

优先采用一种主冷色、一种辅冷色和中性色缓冲。若三种冷色同时出现，让它们分别承担背景、结构和小范围过渡，避免等面积并列。首选配色关系为：

- `浅蓝/青色静区 + #ff4900 结构面`：明亮、通透，橙色集中在下方、边缘或空间断点。
- `深蓝静区 + #ff4900 结构面`：色彩更强，使用水平、斜向或悬浮大形，避免纯黑。
- `雾蓝或深青天空 + 低饱和绿色 + #ff4900 人物/超现实物体`：适合人物与自然载体，让暖色轮廓保持完整而不过量。

米白、浅灰和少量暖灰可作为缓冲。大块纯黑、棕黄滤镜和多种高饱和冷色同时竞争均不符合当前偏好。

## 3. 构图系统

每个方案只选一个主系统，并补充明确的主体位置、视觉路径和留白。默认优先使用写实超现实主义系统：

- `超现实双层空间`：上下或前后两个简洁空间由一条明确边界分开，加入一个悬浮物、异常尺度主体或不连续入口；异常关系只保留一种，材质、透视和投影必须内部一致。
- `冷色大场 + 边角锚点`：约 `55%-75%` 为平静冷色面，橙色位于下方、边缘或色面交界；用尺度反差形成主要张力。
- `平面切割`：用两到三个干净色面按水平、竖直或斜线分割画面，橙色只承担一个色面；边界清晰，禁止碎片化拼贴。
- `线条与投影`：单一栏杆、灯杆、屋檐或构件形成折线、平行线或对角线，硬光阴影作为第二条视觉路径。
- `孤立主体与大环境`：人物、动物或单一物体位于中下部或下方三分区，尺度较小，周围保留大面积天空、水面、草地或墙面。
- `自然三层景深`：前景地表、中景人物/物体、后景坡地/云层自然递进，以斜坡、树冠或倒影形成大轮廓。

构成感来自面积、方向、间距、重复、遮挡和比例关系。至少写清其中三项，不使用“有构成感”作为唯一构图说明。避免同一画面同时出现复杂建筑、碎石地面、砖缝、密集道具和多个人物。

### 3.1 参考图校准后的构图决策

当参考图呈现极简几何或纪实重构语法时，默认按以下顺序取舍：

1. 先确定一个占主导的几何场域，再加入一个可读的偏离点；不要让多个物体平均分配视觉重量。
2. 让重复结构承担节奏，让一次错位、开口、人物、树、投影或暗窗承担焦点；不要同时堆叠多套重复系统。
3. 选择一条主视觉路径：垂直、水平、对角、曲线或汇聚线只能有一个主导方向，其他线条退为辅助。
4. 优先采用近正面或长焦式压缩，保留大块连续负空间；主体集中在下方、边缘或偏轴位置，但不强制把主体排除在顶部，位置以整体协调为准。
5. 需要建筑尺度时，可用一个很小的人物或物件作比例线索，而不是增加更多叙事道具。

## 4. 9:16 壁纸适配

默认按完整 9:16 画面优化：整张图优先保留约 `55%-75%` 的连续冷色静区，顶部不设固定留白比例，允许主体、橙色锚点或视觉路径进入顶部，只要整体重量、节奏和裁切协调。若用户明确指定锁屏或设备模板，再避免人脸、文字、极细线条和主要锚点落入对应界面区域；底部约 `10%-15%` 仅在该场景下作为可选安全余量。

画面要允许少量上下裁切，边缘不要放紧贴画框的关键物件。除非用户明确需要，画面内不生成文字、数字、Logo、界面元素或假水印。

## 5. 写实摄影与电影胶片质感

先写物理条件，再写审美结果：

- 光线：明确光源方向、软硬、时间或室内来源，以及橙色表面的反射方式。
- 材质：说明漆面、织物、玻璃、金属、混凝土、水面等表面的粗糙度与反射特征。
- 镜头：环境与建筑通常使用自然透视的中广角；静物和材质细节使用中长焦或微距。避免无理由的超广角畸变。
- 胶片感：细密有机颗粒、克制高光晕染、柔和高光滚降、略带层次的阴影、自然色偏和微弱镜头呼吸感。

根据主题只选择一种光线模式：

- `清洁硬光`：适合几何、建筑、街景和超现实空间。阳光方向单一，轮廓影与长投影清晰，表面保持干净，不用 HDR 强化纹理。
- `自然胶片光`：适合草地、海岸、人物和云层。中低反差、略柔和的自然光、低饱和绿色和雾蓝层次，曝光保持开阔，不能阴沉欠曝。

胶片颗粒应在暗部与中间调中自然可见，但不能掩盖材质。除非用户明确要求复古损耗，不加入严重漏光、划痕、灰尘、过度褪色或强烈色差。

参考图中较稳定的摄影质感应按以下边界处理：

- 真实日光或环境光优先；硬光用于清晰投影和几何边界，漫射光用于大留白、远景和空气感，单条提示词只指定一个主模式。
- 反差保持中等且受控；只有剪影、深色框中框或单一投影确实服务构图时，才允许局部高反差。
- 维持清晰但不过锐的消费级或中高端相机观感，保留墙面、砖、木、金属、玻璃和水面的自然微纹理。
- 竖向数字拖影、扫描条纹和 glitch 只在用户明确选择实验后期时启用，不能写成默认“胶片感”。

## 6. 常见失败模式

- 橙色面积过大，画面变成品牌海报而非壁纸。
- 超现实机制超过一种，画面变成梦境拼贴、奇幻插画或无逻辑的异常材质堆砌。
- `#ff4900` 漂成高饱和正红、猩红、酒红或大面积红色建筑。
- 浅蓝、深蓝和青色同样饱和，失去主次。
- 橙色只是无缘由的发光块，缺乏真实载体。
- 用重颗粒、低清晰度或棕黄色滤镜模拟“电影感”。
- 用墙面污迹、剥落、砖缝、碎石或复杂地面制造虚假的“摄影细节”。
- 大块纯黑平面吞没冷色层次；自然树影或小型黑色轮廓不在此限。
- 主体正压在屏幕时钟区域，或四周没有裁切余量。
- 同时使用对称、对角、框中框、极简等互相竞争的构图指令。
- 堆叠矛盾摄影词，例如“正午硬光、柔和阴天、霓虹夜景”同时出现。
